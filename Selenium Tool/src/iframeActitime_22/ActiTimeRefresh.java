/*
package iframeActitime_22;

import iframeActitime_22.LoginPage;
public class ActiTimeRefresh extends LoginPage
{

	public static void main(String[] args) throws InterruptedException
	{
		// call the static method
		openApplication("chrome");
		
		
		// create object for Loginpage and call the non static methods present in that class 
		// using reference variable lp which holds the address of the object 
		LoginPage lp = new LoginPage(driver) ;
		lp.setUserName("admin");
		lp.setPassword("manager");
		lp.clickLoginButton();
	
	}
}
*/